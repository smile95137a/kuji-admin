spring:
  datasource:
    # url: jdbc:mysql://localhost:3306/kuji?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false
    url: jdbc:mysql://onekuji-lotery.cdi42o44miez.ap-northeast-1.rds.amazonaws.com:3306/kuji?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=true
    # username: root
    username: admin
    # password: "123456"
    password: "EASONlotery!!"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    root: DEBUG
    org.hibernate.SQL: DEBUG
    # MyBatis SQL 日誌
    com.group.admin.mapper: DEBUG

# application-dev.yml
jwt:
  secret: myDevSecret123

google:
  client-id: ""
