spring:
  datasource:
    url: jdbc:mysql://database-1.clsi2geo699r.ap-northeast-1.rds.amazonaws.com:3306/kuji?useSSL=true&serverTimezone=Asia/Taipei&characterEncoding=UTF-8
    username: admin
    password: WUfan0667.
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: none           # 正式環境不要自動建表
    show-sql: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

logging:
  level:
    root: INFO
    org.hibernate.SQL: WARN
    com.group.admin: INFO
  file:
    name: logs/admin-prod.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# JWT 配置（正式環境使用強密鑰）
jwt:
  secret: ${JWT_SECRET:KUJI_PRODUCTION_SECRET_KEY_2026_VERY_SECURE_DO_NOT_SHARE}
  expiration: 86400000  # 24 小時

# 正式環境 CORS - 允許前端域名
cors:
  allowed-origins: http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://18.179.187.129,http://18.179.187.129:80,http://18.179.187.129:3000,http://18.179.187.129:5173

# Google OAuth
google:
  client-id: ${GOOGLE_CLIENT_ID:}

# AWS S3 配置
# ⚠️ 憑證已移至環境變數，請在啟動前設定：
#   export AWS_ACCESS_KEY_ID=your_access_key
#   export AWS_SECRET_ACCESS_KEY=your_secret_key
aws:
  s3:
    bucket-name: test-ourkuji
    region: ap-northeast-1
    access-key: ${AWS_ACCESS_KEY_ID:}
    secret-key: ${AWS_SECRET_ACCESS_KEY:}
    base-url: https://test-ourkuji.s3.ap-northeast-1.amazonaws.com
