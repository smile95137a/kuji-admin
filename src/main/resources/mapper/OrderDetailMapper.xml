<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group.admin.mapper.OrderDetailMapper">

  <resultMap id="OrderDetailMap" type="com.group.admin.entity.OrderDetail">
    <result column="id" property="id" />
    <result column="bonusPointsEarned" property="bonusPointsEarned" />
    <result column="orderId" property="orderId" />
    <result column="productDetailId" property="productDetailId" />
    <result column="quantity" property="quantity" />
    <result column="resultItemId" property="resultItemId" />
    <result column="storeProductId" property="storeProductId" />
    <result column="totalPrice" property="totalPrice" />
    <result column="unitPrice" property="unitPrice" />
    <result column="productDetailName" property="productDetailName" />
    <result column="productId" property="productId" />
    <result column="resultStatus" property="resultStatus" />
    <result column="storeProductName" property="storeProductName" />
    <result column="billNumber" property="billNumber" />
  </resultMap>

  <select id="selectByExample" resultMap="OrderDetailMap" parameterType="com.group.admin.example.OrderDetailExample">
    SELECT * FROM order_detail
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <select id="countByExample" resultType="long" parameterType="com.group.admin.example.OrderDetailExample">
    SELECT COUNT(*) FROM order_detail
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <delete id="deleteByExample" parameterType="com.group.admin.example.OrderDetailExample">
    DELETE FROM order_detail
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </delete>

</mapper>
