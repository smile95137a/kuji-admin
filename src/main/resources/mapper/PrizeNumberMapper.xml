<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group.admin.mapper.PrizeNumberMapper">

  <resultMap id="PrizeNumberMap" type="com.group.admin.entity.PrizeNumber">
    <result column="prizeNumberId" property="prizeNumberId" />
    <result column="isDrawn" property="isDrawn" />
    <result column="level" property="level" />
    <result column="number" property="number" />
    <result column="productDetailId" property="productDetailId" />
    <result column="productId" property="productId" />
    <result column="probability" property="probability" />
  </resultMap>

  <select id="selectByExample" resultMap="PrizeNumberMap" parameterType="com.group.admin.example.PrizeNumberExample">
    SELECT * FROM prize_number
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <select id="countByExample" resultType="long" parameterType="com.group.admin.example.PrizeNumberExample">
    SELECT COUNT(*) FROM prize_number
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <delete id="deleteByExample" parameterType="com.group.admin.example.PrizeNumberExample">
    DELETE FROM prize_number
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </delete>

</mapper>
