<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group.admin.mapper.PaymentRecordMapper">

  <resultMap id="PaymentRecordMap" type="com.group.admin.entity.PaymentRecord">
    <result column="id" property="id" />
    <result column="amount" property="amount" />
    <result column="createDate" property="createDate" />
    <result column="currencyType" property="currencyType" />
    <result column="paymentMethod" property="paymentMethod" />
    <result column="status" property="status" />
    <result column="transactionDate" property="transactionDate" />
    <result column="transactionId" property="transactionId" />
    <result column="updateDate" property="updateDate" />
    <result column="userId" property="userId" />
  </resultMap>

  <select id="selectByExample" resultMap="PaymentRecordMap" parameterType="com.group.admin.example.PaymentRecordExample">
    SELECT * FROM payment_record
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <select id="countByExample" resultType="long" parameterType="com.group.admin.example.PaymentRecordExample">
    SELECT COUNT(*) FROM payment_record
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <delete id="deleteByExample" parameterType="com.group.admin.example.PaymentRecordExample">
    DELETE FROM payment_record
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </delete>

</mapper>
