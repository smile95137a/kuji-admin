<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group.admin.mapper.CartItemMapper">

  <resultMap id="CartItemMap" type="com.group.admin.entity.CartItem">
    <result column="cartItemId" property="cartItemId" />
    <result column="cartId" property="cartId" />
    <result column="isSelected" property="isSelected" />
    <result column="quantity" property="quantity" />
    <result column="size" property="size" />
    <result column="storeProductId" property="storeProductId" />
    <result column="totalPrice" property="totalPrice" />
    <result column="unitPrice" property="unitPrice" />
    <result column="productDetailId" property="productDetailId" />
    <result column="status" property="status" />
  </resultMap>

  <select id="selectByExample" resultMap="CartItemMap" parameterType="com.group.admin.example.CartItemExample">
    SELECT * FROM cart_item
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <select id="countByExample" resultType="long" parameterType="com.group.admin.example.CartItemExample">
    SELECT COUNT(*) FROM cart_item
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <delete id="deleteByExample" parameterType="com.group.admin.example.CartItemExample">
    DELETE FROM cart_item
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </delete>

</mapper>
