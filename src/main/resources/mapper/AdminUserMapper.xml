<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group.admin.mapper.AdminUserMapper">

  <resultMap id="AdminUserMap" type="com.group.admin.entity.AdminUser">
    <result column="id" property="id" />
    <result column="username" property="username" />
    <result column="password" property="password" />
    <result column="email" property="email" />
    <result column="displayName" property="displayName" />
    <result column="phone" property="phone" />
    <result column="status" property="status" />
    <result column="forceChangePassword" property="forceChangePassword" />
    <result column="lastLoginAt" property="lastLoginAt" />
    <result column="createdBy" property="createdBy" />
    <result column="createdAt" property="createdAt" />
    <result column="updatedBy" property="updatedBy" />
    <result column="updatedAt" property="updatedAt" />
    <result column="remark" property="remark" />
  </resultMap>

  <select id="selectByExample" resultMap="AdminUserMap" parameterType="com.group.admin.example.AdminUserExample">
    SELECT * FROM admin_user
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <select id="countByExample" resultType="long" parameterType="com.group.admin.example.AdminUserExample">
    SELECT COUNT(*) FROM admin_user
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </select>

  <delete id="deleteByExample" parameterType="com.group.admin.example.AdminUserExample">
    DELETE FROM admin_user
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.conditions.size() > 0">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.conditions.entrySet()" item="entry" index="key">
              and ${key} = #{entry.value}
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </delete>

</mapper>
