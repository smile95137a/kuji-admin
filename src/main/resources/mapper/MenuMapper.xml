<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group.admin.mapper.MenuMapper">

  <resultMap id="MenuMap" type="com.group.admin.entity.Menu">
    <result column="id" property="id" />
    <result column="name" property="name" />
    <result column="path" property="path" />
    <result column="parentId" property="parentId" />
    <result column="icon" property="icon" />
    <result column="orderNum" property="orderNum" />
    <result column="createDate" property="createDate" />
    <result column="updateDate" property="updateDate" />
  </resultMap>

  <select id="selectByExample" resultMap="MenuMap" parameterType="com.group.admin.entity.example.MenuExample">
    SELECT * FROM menu
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <foreach collection="criteria.conditions.entrySet()" item="entry">
          ${entry.key} = #{entry.value} AND
        </foreach>
      </foreach>
    </where>
  </select>
</mapper>
